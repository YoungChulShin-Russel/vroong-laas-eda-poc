package vroong.laas.delivery.core.domain.routing;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RoutingTemplateRepository extends JpaRepository<RoutingTemplate, Long> {
}
